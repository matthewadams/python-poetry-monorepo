[tool.poetry]
name = "sample-python-monorepo"
version = "0.12.0-dev.0"
description = "sample python monorepo"

[tool.poetry.dependencies]
python = ">=3.13"

[build-system]
requires = ["poetry-core>=2.0.1,<2.1.0"]
build-backend = "poetry.core.masonry.api"
